# Public site URL used to build same-deployment Commerce and CHIP bridge routes.
PUBLIC_SITE_URL=https://your-domain.example

# Server-only HMAC secret shared by Commerce Core and the CHIP plugin.
# Generate with: openssl rand -hex 32
COMMERCE_BRIDGE_SECRET=
